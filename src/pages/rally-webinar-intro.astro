---

function shouldRedirectNow(now = new Date()) {
  const y = now.getUTCFullYear();
  const m = now.getUTCMonth(); // 0 = Jan → 9 = Oct
  const d = now.getUTCDate();
  const wd = now.getUTCDay(); // 4 = Thursday
  const h = now.getUTCHours();

  const isTargetTime = () => {
  const now = new Date();
  const y = now.getUTCFullYear();
  const m = now.getUTCMonth();
  const d = now.getUTCDate();
  const wd = now.getUTCDay();
  const h = now.getUTCHours();
  const min = now.getUTCMinutes();

  // Option 1: Thursday, 30 Oct 2025, 13:00–16:00 GMT
  const isMainDate = y === 2025 && m === 9 && d === 30 && wd === 4 && h >= 13 && h < 16;

  // Option 2: 24 Oct 2025, 18:00–18:15 BST (BST = GMT+1, so 17:00–17:15 UTC)
  const isAltDate =
    y === 2025 && m === 9 && d === 24 &&
    ((h === 17 && min >= 0) || (h === 17 && min < 15));

  return isMainDate || isAltDate;
};
}


if (shouldRedirectNow()) {
  return new Response(null, {
    status: 302,
    headers: { Location: targetUrl },
  });
}


return new Response(
  `<!doctype html>
  <html lang="en">
    <head>
      <meta charset="utf-8" />
      <title>Join Link</title>
      <meta name="viewport" content="width=device-width, initial-scale=1" />
    </head>
    <body>
      <script>
        alert("The join link is only active on Thursday, 30 October 2025 from 13:00–16:00 GMT.");
        window.location.href = "https://www.youtube.com/watch?v=dQw4w9WgXcQ";
      </script>
    </body>
  </html>`,
  {
    headers: { "Content-Type": "text/html; charset=utf-8" },
  }
);
---
